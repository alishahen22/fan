# Activity Log - دليل الاستخدام

## نظام تتبع الأنشطة (Activity Log)

تم تثبيت وإعداد نظام تتبع الأنشطة بنجاح في التطبيق. هذا النظام يقوم بتسجيل عمليات الإنشاء والتحديث والحذف التي تتم من خلال **الداشبورد فقط**.

## المميزات المتوفرة:

### 🎯 **مميزة جديدة: تتبع أنشطة المشرفين**

-   **أيقونة في صفحة المشرفين** توجه لصفحة أنشطة كل مشرف
-   **فلترة حسب الفترة**: هذا الشهر / في العموم
-   **عرض عمليات الإنشاء فقط** مع فحص الصلاحيات
-   **تصدير التقارير** لأنشطة كل مشرف

### 1. تتبع تلقائي للأنشطة

-   يتم تسجيل عمليات **إنشاء** البيانات الجديدة
-   يتم تسجيل عمليات **تحديث** البيانات الموجودة
-   يتم تسجيل عمليات **حذف** البيانات
-   يتم حفظ معلومات المستخدم الذي قام بالعملية

### 2. النماذج المشمولة بالتتبع:

تم إضافة ActivityLoggable trait للنماذج التالية:

-   User - المستخدمين
-   Admin - المديرين
-   Product - المنتجات
-   Category - الفئات
-   Order - الطلبات
-   Setting - الإعدادات
-   Package - الحزم
-   وجميع النماذج الأخرى في التطبيق (44 نموذج)

### 3. المعلومات المسجلة:

-   **اسم المستخدم** الذي قام بالعملية
-   **نوع العملية** (إنشاء/تحديث/حذف)
-   **النموذج المتأثر** والمعرف الخاص به
-   **التغييرات المحددة** (في حالة التحديث)
-   **تاريخ ووقت** العملية
-   **عنوان IP** الخاص بالمستخدم
-   **متصفح المستخدم** (User Agent)

## طرق الوصول لسجلات الأنشطة:

### 1. صفحة عامة لسجلات الأنشطة:

```
/admin/activity-logs
```

### 2. صفحة أنشطة المشرف المحدد:

```
/admin/admins/{admin_id}/activity
```

تُفتح من خلال أيقونة التاريخ في صفحة المشرفين

### 3. من خلال الكود:

```php
// عرض آخر 10 أنشطة
$activities = Activity::latest()->take(10)->get();

// عرض أنشطة مشرف معين
$adminActivities = Activity::causedBy($admin)->get();

// عرض أنشطة نموذج معين
$productActivities = Activity::forSubject($product)->get();
```

## صفحات الاختبار:

### 1. اختبار النظام:

```
/test-activity-log
```

هذه الصفحة تقوم بإنشاء بيانات تجريبية لاختبار تسجيل الأنشطة

### 2. عرض السجلات:

```
/view-activity-logs
```

صفحة لعرض جميع سجلات الأنشطة بشكل مرتب ومنسق

## إعدادات إضافية:

### 1. في ملف .env:

```env
# تفعيل/إيقاف تسجيل الأنشطة
ACTIVITY_LOGGER_ENABLED=true

# اسم الجدول
ACTIVITY_LOGGER_TABLE_NAME=activity_log

# اتصال قاعدة البيانات
ACTIVITY_LOGGER_DB_CONNECTION=mysql
```

### 2. تنظيف السجلات القديمة:

```bash
# حذف السجلات الأقدم من 30 يوم
php artisan activitylog:clean --days=30

# أو من خلال الداشبورد
```

### 3. إضافة تسجيل مخصص:

```php
// في أي Controller
activity()
    ->causedBy(auth()->user())
    ->log('تم تنفيذ عملية مخصصة');

// مع تفاصيل إضافية
activity()
    ->causedBy(auth()->user())
    ->withProperties(['key' => 'value'])
    ->log('تم تنفيذ عملية مع بيانات إضافية');
```

## المزايا الإضافية:

### 1. Middleware للتتبع التلقائي:

تم إضافة ActivityLogMiddleware للتتبع التلقائي لجميع العمليات

### 2. Command لإضافة التتبع للنماذج:

```bash
php artisan models:add-activity-logging
```

### 3. Observer عالمي:

تم إنشاء Observer يعمل على جميع النماذج التي تستخدم ActivityLoggable trait

## ملاحظات مهمة:

1. **الأمان**: جميع العمليات محمية ولا يمكن الوصول إليها إلا من المستخدمين المصرح لهم
2. **الأداء**: النظام محسن للأداء ولا يؤثر على سرعة التطبيق
3. **التخزين**: السجلات تحفظ في جدول منفصل `activity_log`
4. **التنظيف**: يمكن حذف السجلات القديمة تلقائياً أو يدوياً

## استكشاف الأخطاء:

إذا لم يعمل النظام كما هو متوقع:

1. تأكد من تشغيل migrations:

```bash
php artisan migrate
```

2. تأكد من وجود ActivityLoggable trait في النماذج:

```bash
grep -r "ActivityLoggable" app/Models/
```

3. تحقق من إعدادات .env للتأكد من تفعيل النظام

---

تم إنشاء هذا النظام لتتبع جميع العمليات في الداشبورد بشكل تلقائي وشامل.
